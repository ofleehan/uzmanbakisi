// Shared configuration values for the application

export const SPHERE_RADIUS = 300; // in pixels - Reduced for better initial visibility on smaller screens
export const CARD_WIDTH = 200;    // in pixels
export const CARD_HEIGHT = 112;    // in pixels